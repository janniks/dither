{
  "name": "echo-config",
  "version": "0.0.1",
  "description": "Test fixture: reads env values via readInput() and echoes them.",
  "license": "MIT",
  "dither": {
    "display_name": "Echo Config",
    "tagline": "Echoes its env values to verify the SDK round-trip.",
    "env": [
      {
        "name": "GREETING",
        "description": "A non-secret string the plugin will echo."
      },
      {
        "name": "MAX_RUNS",
        "default": "3"
      },
      {
        "name": "API_TOKEN",
        "description": "A secret-like value the plugin will echo (proves delivery)."
      }
    ],
    "create": [
      "echoed"
    ]
  }
}
