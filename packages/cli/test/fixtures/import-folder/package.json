{
  "name": "import-folder",
  "version": "0.0.1",
  "description": "Test fixture plugin: writes a single entry via the SDK.",
  "license": "MIT",
  "dither": {
    "display_name": "Import Folder",
    "tagline": "Test fixture for the plugin SDK.",
    "create": [
      "imported"
    ]
  }
}
